apiVersion: v1
kind: Service
metadata:
  name: developer-application
  namespace: ilay
spec:
  selector:
    app: developer-application
  ports:
    - name: http
      port: 8080
      targetPort: 8080
