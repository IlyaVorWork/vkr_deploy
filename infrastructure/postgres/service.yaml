apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: appstore
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None  # headless — StatefulSet pods адресуются по postgres-0.postgres
